import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* App lives at the repository root, so Next/Turbopack detects it with no
     extra configuration. */
};

export default nextConfig;
